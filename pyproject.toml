[project]
name = "lejepa-tutorials"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["stable-pretraining"]

[tool.setuptools]
py-modules = ["app", "debug_lewm", "inet10", "mmnist", "utils"]

[tool.ruff]
line-length = 100
